# 実装状況 (Select Navi)

最終更新：MVPフェーズ0（基本ナビ＋IC登録による下道自動ルート）実装・動作確認完了時点

## 現在の状態

MVPスコープ「①基本ナビ＋②IC登録による下道自動ルート」をNext.jsアプリとして実装し、実際にブラウザを操作するテスト（Playwright）で動作確認済み。デプロイはまだで、ローカル開発環境（`npm run dev`）でのみ動作確認している。

## 使っているサービスの役割分担

| 役割 | サービス |
|---|---|
| 地図の表示（タイル描画） | Mapbox GL JS |
| ルート計算（2点間の道のり・所要時間算出） | Mapbox Directions API |
| 地名・IC名の検索（テキスト→座標変換） | Google Places API (New) |
| 現在地取得 | ブラウザ標準 Geolocation API |
| 音声案内 | ブラウザ標準 Web Speech API (SpeechSynthesis) |

「地図の見た目とルート計算はMapbox、地名からの座標変換だけはGoogle」というハイブリッド構成。理由はMapbox Geocodingが日本の駅名・IC名の検索に弱かったため（詳細は`TECH_STACK.md`参照）。

## 実装済み機能

- **出発地・目的地の検索**：オートコンプリート付き検索欄（Google Places API経由）
- **IC経由ルート指定**：「IC経由で下道ルートも指定する」チェックボックス→乗りたいIC・降りたいICを検索指定
- **ルート計算**
  - IC未指定時：出発地→目的地の通常ルート（高速道路を含む最短経路）
  - IC指定時：3区間ルートを自動算出
    1. 出発地→乗りたいIC（下道のみ。`exclude=motorway`で高速を明示的に除外）
    2. 乗りたいIC→降りたいIC（高速道路ルート）
    3. 降りたいIC→目的地（下道のみ。同様に高速除外）
- **地図表示**：区間ごとに色分けして描画（下道=青、高速=赤）、ルート全体が収まるよう自動ズーム
- **ルート概要パネル**：区間ごとの距離・時間、合計距離・時間
- **高速料金の目安表示**：IC区間（高速区間）について、NEXCO標準料金式（距離×24.6円/km＋ターミナルチャージ150円、消費税込み、10円単位で丸め）から軽自動車等・普通車・中型車・大型車・特大車の5車種分を算出して表示。本四高速・首都高速・阪神高速など特殊区間の実料金とは異なる旨を明記した「あくまで目安」の位置づけ
- **出発地に現在地を指定**：「📍現在地を出発地にする」ボタンでブラウザのGeolocation APIから一度だけ現在地を取得し、出発地欄に自動セット（名称は「現在地」と表示）
- **ナビ開始／終了**
  - 現在地をGeolocation APIでリアルタイム取得し、地図上にマーカー表示
  - Mapbox Directionsから取得したturn-by-turnの案内文をWeb Speech APIで音声読み上げ
  - 現在地とマニューバ地点の距離（Haversine公式で自前計算）が40m以内になったら次の案内へ自動的に進行
  - 位置情報の取得に失敗した場合（許可拒否・取得不可・タイムアウト）は、原因に応じたわかりやすいエラーメッセージを画面に表示

## 動作確認の方法・結果

- Playwright（ヘッドレスChromium）でブラウザ操作を自動化してテスト
- テストケース：
  - 徳島駅→亀岡駅の通常ルート検索
  - 徳島駅→亀岡駅、鳴門IC経由・垂水IC下車の3区間ルート検索
- 両ケースとも地図描画・ルート概要表示まで正常動作、コンソールエラーなしを確認

## テスト中に発覚し、修正済みの問題

1. **Mapbox Geocodingの日本国内精度不足**：「東京駅」のような有名地名すら正しくヒットしない根本的な限界があったため、地名・IC検索をGoogle Places API (New)に切替（経緯は`TECH_STACK.md`参照）
2. **「下道」区間が高速道路を含み得た**：Mapbox Directionsの`exclude=motorway`オプションを指定し、IC区間を跨がない区間は確実に下道のみになるよう修正
3. **高速料金の正確な算出には無料の公式APIが存在しない**：NAVITIME APIは正確だが直接契約が必要な商用API。NEXCO標準区間の計算式（距離×24.6円/km＋150円）は無料で使えるが、本四高速・首都高速など特殊区間には対応していないため、あくまで目安として実装（詳細は`TECH_STACK.md`参照）

## 未実装・今後の作業

- **AWS Amplify（Lambda + DynamoDB）への実デプロイ**：現状はNext.jsのAPI Routesでロジックを実装しており、AWS上への実際のプロビジョニングは未着手
- **③地元民ルート判定**：位置情報の集計によるローカルルート可視化機能（MVP対象外として保留中）
- **④事故・工事・故障者通報＋周辺車両への通知**（MVP対象外として保留中）
- IC名検索の網羅性の検証（表記揺れなどでGoogle Places APIでも見つからないICがないか）
- 本番運用に向けたAPIキーの制限強化（AWSデプロイ後、固定の送信元IPが判明してから見直し）

## 関連ドキュメント

- 技術選定の理由・アーキテクチャ図・ファイル構成図：`TECH_STACK.md`
