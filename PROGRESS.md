# 実装状況 (Select Navi)

最終更新：UIをシステム/HUD風デザインに刷新し、本番（AWS Amplify）へのデプロイ・動作確認が完了した時点

## 現在の状態

MVPスコープの主要機能に加え、本番環境（AWS Amplify Hosting）へのデプロイも完了し、公開URL（`https://main.dmm1g4zudj9sa.amplifyapp.com`）で実際に動作している。DynamoDBを使った通報機能・ルート選択実績の記録機能、PWA対応、システム/HUD風のUIデザインまで実装済み。

## 使っているサービスの役割分担

| 役割 | サービス |
|---|---|
| 地図の表示（タイル描画） | Mapbox GL JS（オリジナルのカスタムスタイル） |
| ルート計算（2点間の道のり・所要時間算出） | Mapbox Directions API |
| 地名・IC名の検索（テキスト→座標変換） | Google Places API (New) |
| 現在地取得 | ブラウザ標準 Geolocation API |
| 音声案内 | ブラウザ標準 Web Speech API (SpeechSynthesis) |
| ホスティング・APIルート実行 | AWS Amplify Hosting（Next.js SSR） |
| データ保存 | AWS DynamoDB（通報データ／ルート選択実績データ） |

「地図の見た目とルート計算はMapbox、地名からの座標変換だけはGoogle」というハイブリッド構成。理由はMapbox Geocodingが日本の駅名・IC名の検索に弱かったため（詳細は`TECH_STACK.md`参照）。

## 実装済み機能

### ルート検索・ナビゲーション
- **出発地・目的地の検索**：オートコンプリート付き検索欄（Google Places API経由）
- **通常検索（最短ルート）**：IC・下道回避のどちらも指定しない場合、高速道路を含む最短ルートを表示
- **「高速道路を使わない」チェックボックス**：オンにすると自動的に下道のみのルートへリルートする。ONの間はIC指定機能は非表示になり、互いに排他的
- **IC経由ルート指定**：「IC経由で下道ルートも指定する」チェックボックス→乗りたいIC・降りたいICを検索指定
  - IC専用の検索欄では、入力語に「IC」「インターチェンジ」等が含まれていない場合、検索時に自動で「インターチェンジ」を付加してGoogle Places APIに問い合わせる（例：「鳴門」→「鳴門 インターチェンジ」で検索）
  - IC指定時は3区間ルートを自動算出：①出発地→乗りたいIC（下道のみ）②乗りたいIC→降りたいIC（高速道路）③降りたいIC→目的地（下道のみ）
- **ルート概要パネル**：区間ごとの距離・時間、合計の推定所要時間・距離をタイル表示。区間ラベルは「高速区間」「下道区間」「ルート」（通常検索など下道/高速が明確に分かれない場合）を状況に応じて出し分け
- **高速料金の目安表示**：IC区間（高速区間）について、NEXCO標準料金式（距離×24.6円/km＋ターミナルチャージ150円、消費税込み、10円単位で丸め）から軽自動車等・普通車・中型車・大型車・特大車の5車種分を算出して表示。本四高速・首都高速・阪神高速など特殊区間の実料金とは異なる旨を明記した「あくまで目安」の位置づけ
- **出発地に現在地を指定**：「現在地を出発地にする」ボタンでブラウザのGeolocation APIから一度だけ現在地を取得し、出発地欄に自動セット
- **ナビ開始／終了**
  - 現在地をGeolocation APIでリアルタイム取得し、地図上に車の形をしたマーカーで進行方向を表示
  - Mapbox Directionsから取得したturn-by-turnの案内文をWeb Speech APIで音声読み上げ
  - 現在地とマニューバ地点の距離（Haversine公式で自前計算）が40m以内になったら次の案内へ自動的に進行
  - 位置情報の取得に失敗した場合（許可拒否・取得不可・タイムアウト）は、原因に応じたわかりやすいエラーメッセージを画面に表示
- **ナビ中のカメラ視点**：ナビ開始中は地図が現在地を中心にズーム・傾き・進行方向への回転を伴って追従し、実際のカーナビのような視点になる。ナビ終了時は元の見下ろし視点に戻る
- **ナビ中の自動リルート**：現在地がルート線から50m以上離れた状態が8秒以上続いたら逸脱と判定し、現在地を新しい出発地として自動的にルートを引き直す。IC区間の手前で外れた場合はIC指定を維持し、それ以降で外れた場合はIC指定なしで直接再計算する

### 通報機能（事故・渋滞・工事）
- ナビ中の画面右下に「事故」「渋滞」「工事」の3つの通報ボタンを表示（それぞれ赤・緑・金の枠線色で色分け）
- 通報するとDynamoDB（`SelectNavi-Reports`テーブル）に現在地・種別・タイムスタンプを保存。TTL属性により15分で自動的に無効化される
- ナビ中は半径3000m以内の新着通報を定期的にポーリングして取得し、地図上にアラート表示（距離付き）

### ルート選択実績の記録（データ収集基盤）
- 将来的な差別化（「実際に多くのユーザーが選んで走り切ったIC組み合わせ」の可視化）に向けた土台として、「ナビ開始」を押した時点でルート選択の内容（出発地・目的地・方式「IC指定/高速回避/最速」・IC区間・距離時間）をDynamoDB（`SelectNaviRouteChoices`テーブル、TTLなしで恒久保存）に記録
- ログイン機能がないため、端末ごとの匿名ID（UUID、localStorage管理）で記録を紐づけ
- 「ナビ終了」まで到達した場合は`completed: true`で更新し、そのルート選択が実際に使い切られたかを区別できるようにしている

### UIデザイン
- フォントに「Anta」（ロゴ見出し）・「Jaro」（アイキャッチラベル）・「JetBrains Mono」（ラベル・入力欄・ボタン等の等幅UI文字）・「Outfit」（本文）を採用した、クリーム×オレンジ基調の「システム/HUD」風デザイン
- チェックボックスはオレンジに光るスライド式トグルスイッチ、入力欄は入力済みでオレンジに光るドット付き
- ステータス表示（「SYSTEM READY」「SEARCHING...」「NAVIGATING」）が実際のアプリの状態と連動
- ルートライン・現在地マーカーもオレンジ（メインルート）／インク色（高速区間）に統一
- 地図自体もMapbox Studioでオリジナルにカスタムした、クリーム×グレー基調の落ち着いた配色スタイルを使用

### PWA対応
- ホーム画面への追加に対応（`app/manifest.ts`、各種サイズのアイコン）
- ホーム画面から起動した際、ブラウザのアドレスバー等がない「アプリらしい」見た目で起動（オフライン対応・Service Workerは対象外の「軽量PWA」）

### インフラ・AWSデプロイ
- AWS Amplify Hosting（Next.js SSR、モノレポ構成）で本番稼働中
- DynamoDBは2テーブル運用（通報用・ルート選択実績用）。SSR実行時のDynamoDBアクセスは専用の「SSR Compute role」（IAM）で許可
- ゼロ支出予算のアラートをAWS Budgetsに設定済み

## 動作確認の方法・結果

- Playwright（ヘッドレスChromium）でブラウザ操作を自動化してテスト（ローカル・本番URLの両方で実施）
- 主なテストケース：通常ルート検索、IC経由ルート検索、高速道路を使わないルート検索、ナビ開始〜終了、通報ボタンの表示
- いずれも地図描画・ルート概要表示・ナビ動作まで正常動作、コンソールエラーなしを確認

## 未実装・今後の作業

- **ローカル開発環境での`/api/route-choices`の500エラー**：本番用のIAM権限はDynamoDBの2テーブル分あるが、ローカル開発用のアクセスキーがルート選択実績テーブル分は未発行のため、`npm run dev`時にこのAPIだけ失敗する（本番は正常動作）
- **③地元民ルート判定**：位置情報の集計によるローカルルート可視化機能（MVP対象外として保留中。「ルート選択実績の記録」がこの土台にもなる想定）
- **キャラクター（ナビ音声案内用のオリジナルエージェント）機能**：スコープ確定待ちで未着手
- **マネタイズ・ユーザー獲得施策の実行**：広告・競合調査等の議論は実施済みだが、具体的な実装・施策実行は未着手
- Mapboxアクセストークンの分離（ブラウザ用/サーバー専用）：未対応
- IC名検索の網羅性の検証（表記揺れなどでGoogle Places APIでも見つからないICがないか）
- 本番運用に向けたAPIキーの制限強化（送信元ドメイン等でのさらなる絞り込み）

## 関連ドキュメント

- 技術選定の理由・アーキテクチャ図・ファイル構成図：`TECH_STACK.md`
- AWSデプロイ作業の詳細ログ・トラブルシューティング：`AWS.md`
